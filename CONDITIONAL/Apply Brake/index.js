// Sample input
let distance = 100;
let time = 2;

// Calculate speed
let speed = distance / time;

// Check if speed is greater than 40
if (speed > 40) {
    console.log("Apply Brake");
} else {
    console.log("Keep Going");
}
